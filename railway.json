{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py collectstatic --noinput && python manage.py migrate && gunicorn ferreteria_ecommerce.wsgi:application --bind :$PORT --workers 4",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100
  }
}